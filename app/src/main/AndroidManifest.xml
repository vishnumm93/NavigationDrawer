<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.invis.navigationdrawer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Profile_Activity" />
        <activity android:name=".Frofile_Activity" />
        <activity android:name=".Places_Activity" />
        <activity android:name=".Notification_Activity" />
        <activity android:name=".About_Activity" />
        <activity android:name=".Settings_Activity" />
        <activity android:name=".Favourites_Activity" />
        <activity android:name=".Logout_Activity"></activity>
    </application>

</manifest>